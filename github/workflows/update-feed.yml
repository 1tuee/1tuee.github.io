name: Update Game Feed Daily

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 0 * * *'

jobs:
  update-feed:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Fetch Live Game Feed Data
        run: |
          curl -s "https://gamemonetize.com/feed.php?format=0&num=50&page=1" > games.json

      - name: Commit and Push Updates
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add games.json
          git diff-index --quiet HEAD || (git commit -m "Automated daily game feed refresh" && git push)
